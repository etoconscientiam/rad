/**
 * Контакты мастерской. Одинаковы на всех языках, поэтому живут в конфиге,
 * а не в словарях.
 *
 * ВНИМАНИЕ: значения взяты из прототипа и владельцем НЕ подтверждены —
 * это открытый вопрос №4 в docs/04-repo-review.md. Перед публичным запуском
 * сверить, особенно телефон.
 */
export const WORKSHOP = {
  phone: '+995 551 23 45 67',
  email: 'hi@rakurs.ge',
} as const;

/** Телефон в виде, пригодном для href="tel:". */
export const phoneHref = `tel:${WORKSHOP.phone.replace(/[^\d+]/g, '')}`;
